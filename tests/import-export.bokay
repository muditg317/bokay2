// imports:
//  import WholeLib, {oneMethod, SomeClass, anotherMethod} from "file/to/import.bokay"
//    should be similar to JS/TS style
//    extracts only certain symbols into the current context or all symbols under the WholeLib namespace
//    access with WholeLib::oneMethod(...)
import WholeLib, {oneMethod, aField, anotherMethod} from "./export-stuff.bokay";

hello: u8 = aField + 2;
s16 doThing(a: u8, b: u8) {
  return a + b;
}
result: f32 = doThing(WholeLib::indirectMethod(hello), oneMethod(hello));

// TODO: exports:
//  export { method, someField, anotherMethod };
//    should be similar to JS/TS style
//    exported fields are exported as copies, not the same reference
//    WholeLib import only captures the exported symbols and not the rest of the file

// export { hello, doThing };
