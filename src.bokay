type foo = *u8[5,6][2]

type bar = u8[5,6]

type f = u16
type baz = bar[2]

type bo = *baz

type baz = **foo[33]

type bruh = func (bit: foo, bat: func (ping: bool, pong: *bool) => f64) => void;

{{};}

foo: u8 = 5;

bar: mut func(a:u8) => bool {
  b: u8 = a + 3;
  return b % 5 == a%4;
}

bar = func(a:u8) => bool {
  return a < 10;
}

a: bool = bar(foo);

type b = bool;

b: mut b;

b = func() => bool {
  return false;
}()